
@model IEnumerable<FoodOrder.Models.Order.OrderModel>

@{
    ViewData["Title"] = "order Listed";
}
<h3>
   danh sách hóa đơn
</h3>
<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Mã đơn hàng</th>
            <th scope="col">Tên</th>
            <th scope="col">Ngày đặt hàng</th>
            <th scope="col">Trạng thái</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.OrderCode</td>

                <td>@item.UserName</td>
                <td>@item.CreateDate.ToString("dd/MM/yyyy HH:mm:ss")</td>
                <td>
                    @if (item.Status == 1)
                    {
                        <span class=" text-primary">Đơn hàng mới</span>
                    }
                    else if (item.Status == 0)
                    {
                        <span class=" text-success">Đã hoàn thành</span>
                    }
                    else
                    {
                        <span class=" text-danger">Đa hủy</span>
                    }
                </td>
                <td>
                    <a asp-action="ViewOrder" asp-controller="Order" asp-route-ordercode="@item.OrderCode" class="btn btn-warning">ViewOrder </a>
                    <a asp-action="Delete" asp-controller="Order" asp-route-Id="@item.Id" class="btn btn-danger confirmDeletion">Delete </a>
                </td>
            </tr>

        }
    </tbody>
</table>
